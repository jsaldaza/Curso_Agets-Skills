{
  "name": "ai-development-agents-template",
  "version": "1.0.0",
  "private": true,
  "description": "Domain-agnostic AI agent template for software development workflows",
  "scripts": {
    "dev": "node apps/course-site/server.js",
    "build": "echo \"Static course app: no build step required\"",
    "start": "node apps/course-site/server.js",
    "expense:start": "node src/cli.js",
    "lint": "echo \"Define the lint command for this project\"",
    "format": "echo \"Define the format command for this project\"",
    "test": "node --test tests/unit/*.test.js tests/course/*.test.js",
    "test:watch": "echo \"Define the watch test command for this project\"",
    "test:ci": "npm test",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:allure": "playwright test && npm run allure:report",
    "allure:report": "allure generate ./allure-results --clean -o ./allure-report",
    "allure:open": "allure open ./allure-report",
    "template:cleanup:dry": "node scripts/template-cleanup.js",
    "template:cleanup": "node scripts/template-cleanup.js --apply",
    "validate": "npm run test:ci",
    "prepare": "echo \"Optional: add git hooks or setup steps\""
  },
  "keywords": [
    "ai",
    "automation",
    "workflow",
    "agents",
    "template"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "allure-commandline": "^2.38.1",
    "allure-playwright": "^3.6.0"
  }
}
